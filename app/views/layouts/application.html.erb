<!DOCTYPE html>
<html>
  <head>
    <title>OwnCoin</title>
    <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track' => true %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track' => true %>
    <%= csrf_meta_tags %>
  </head>
  <body>
    <nav class="navbar navbar-default navbar-static-top">
      <div class="container">
        <div class="navbar-header">
          <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#navbar" aria-expanded="false" aria-controls="navbar">
            <span class="sr-only">Toggle navigation</span>
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
          </button>
          <a class="navbar-brand" href="/"><%= image_tag 'owncoin_navbar.png', id: 'navbar-icon' %></a>
        </div>
        <div id="navbar" class="navbar-collapse collapse">
          <ul class="nav navbar-nav">
            <li><%= link_to 'Home', home_path %></li>
            <li><%= link_to 'Addresses', home_addresses_path %></li>
            <li><%= link_to 'Wallet', wallet_path %></li>
          </ul>
          <ul class="nav navbar-nav navbar-right">
            <li><%= link_to "#{icon('sign-out')} Logout".html_safe, logout_path, method: :delete %></li>
          </ul>
        </div>
      </div>
    </nav>
    <div class="container">
      <div class="row">
        <div class="information col-xs-3">
          <h4>Information : </h4>
          <div class="balance-view">
            <%= icon('btc') %> <%= @information['balance'] %>
          </div>
          <div class=""><%= icon('link') %> <%= @information['connections'] %> connections</div>
        </div>
        <div class="balance col-xs-9">
          <%= yield %>
        </div>
      </div>
    </div>
  </body>
</html>
